# Variáveis de ambiente para o WebPosto MCP Server

# URL da API do WebPosto (não altere se não for necessário)
WEBPOSTO_API_URL=https://web.qualityautomacao.com.br/INTEGRACAO

# Chave da API do WebPosto (substitua pela sua chave)
WEBPOSTO_API_KEY=SUA_API_KEY_AQUI
